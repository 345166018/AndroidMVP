package com.hongx.hxmvp3.app;

/**
 * 只是一个标识信息，这里的所有信息都是后面用来做KEY的
 */
public enum ConfigKeys {
    API_HOST,//主机地址
    APPLICATION_CONTEXT,//ApplicationContext
    INTERCEPTOR,//拦截器
    CONFIG_READY//标识配置是否可用
}
